seed: 0
dataset_name: hpancreas

data:
  train_h5ad: data/hpancreas/demo_train_geneformer.h5ad
  test_h5ad: data/hpancreas/demo_test_geneformer.h5ad
  celltype_col: Celltype
  gene_name_col: null
  val_fraction: 0.1

pretrained_model_dir: pretrained_model_weights/Geneformer/Geneformer-V1-10M

loss: cross_entropy

rare_class:
  relative_threshold: 0.05

train:
  epochs: 15
  batch_size: 12
  eval_batch_size: 12
  lr: 5.0e-5
  amp: true

model:
  dropout: 0.1

wandb:
  project: sc-imbalance
  entity: null
  run_name: null

output:
  save_dir: results/geneformer_hpancreas_baseline
